<!-- CPU worker 2 VM: PXE boots from Ironic, gets Ubuntu -->
<!-- Requires Linux host with libvirt system daemon (qemu:///system) -->
<domain type="kvm">
  <name>neo-worker-cpu2</name>
  <memory unit="GiB">10</memory>
  <vcpu>4</vcpu>
  <os>
    <type arch="x86_64">hvm</type>
    <!-- PXE boot first, then disk (BIOS mode) -->
    <boot dev="network"/>
    <boot dev="hd"/>
  </os>
  <features>
    <acpi/>
    <apic/>
  </features>
  <cpu mode="host-passthrough"/>
  <devices>
    <!-- OS disk (blank, Ironic writes Ubuntu here) -->
    <disk type="file" device="disk">
      <driver name="qemu" type="qcow2"/>
      <source file="NEO_DATADIR/neo-worker-cpu2.qcow2"/>
      <target dev="vda" bus="virtio"/>
    </disk>
    <!-- NIC 1: provisioning network (PXE boot) -->
    <interface type="network">
      <source network="neo-provisioning"/>
      <mac address="52:54:00:aa:bb:03"/>
      <model type="virtio"/>
    </interface>
    <!-- NIC 2: baremetal network -->
    <interface type="network">
      <source network="neo-baremetal"/>
      <model type="virtio"/>
    </interface>
    <!-- NIC 3: default NAT (internet access for apt-get) -->
    <interface type="network">
      <source network="default"/>
      <model type="virtio"/>
    </interface>
    <serial type="pty">
      <target port="0"/>
    </serial>
    <console type="pty">
      <target type="serial" port="0"/>
    </console>
    <graphics type="vnc" port="-1" autoport="yes"/>
  </devices>
</domain>
