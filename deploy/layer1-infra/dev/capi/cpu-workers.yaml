# CPU worker MachineDeployment for dev
---
apiVersion: cluster.x-k8s.io/v1beta1
kind: MachineDeployment
metadata:
  name: workload-cluster-cpu-workers
  namespace: metal3
spec:
  clusterName: workload-cluster
  replicas: 1
  selector:
    matchLabels:
      cluster.x-k8s.io/cluster-name: workload-cluster
      pool: cpu
  template:
    metadata:
      labels:
        cluster.x-k8s.io/cluster-name: workload-cluster
        pool: cpu
    spec:
      clusterName: workload-cluster
      version: v1.35.3
      bootstrap:
        configRef:
          apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
          kind: KubeadmConfigTemplate
          name: workload-cluster-cpu-workers
      infrastructureRef:
        apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
        kind: Metal3MachineTemplate
        name: workload-cluster-cpu-workers
---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: Metal3MachineTemplate
metadata:
  name: workload-cluster-cpu-workers
  namespace: metal3
spec:
  template:
    spec:
      hostSelector:
        matchLabels:
          feature: cpu
      image:
        url: "http://172.16.20.10:6180/images/ubuntu-worker.img"
        checksum: "http://172.16.20.10:6180/images/ubuntu-worker.img.sha512sum"
        checksumType: sha512
        format: qcow2
---
apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
kind: KubeadmConfigTemplate
metadata:
  name: workload-cluster-cpu-workers
  namespace: metal3
spec:
  template:
    spec:
      users:
        - name: neo
          sudo: ALL=(ALL) NOPASSWD:ALL
          passwd: "$6$neo$4g3zp5Aa8lEJGv3Mv1WRZmweP1eWZSPhITrr38FZc19UKn3l9alJ3eGLGxwdroMGBL/ZW17aZGJhFN.fAh52l1"
          lockPassword: false
          shell: /bin/bash
      preKubeadmCommands:
        - echo 'neo:neo' | chpasswd
        # Bring up all interfaces with DHCP (needed for internet via NAT NIC)
        - |
          cat > /etc/netplan/99-all-dhcp.yaml << 'EOF'
          network:
            version: 2
            ethernets:
              allnics:
                match:
                  name: "en*"
                dhcp4: true
          EOF
        - chmod 600 /etc/netplan/99-all-dhcp.yaml
        - netplan apply || true
        - sleep 5
        # Ensure baremetal NIC is up for cluster communication
        - ip link set ens4 up || true
        - modprobe br_netfilter && modprobe overlay
        - sysctl -w net.bridge.bridge-nf-call-iptables=1
        - sysctl -w net.ipv4.ip_forward=1
        - apt-get update && apt-get install -y apt-transport-https ca-certificates curl containerd
        # Configure containerd with systemd cgroup driver
        - mkdir -p /etc/containerd
        - containerd config default | sed 's/SystemdCgroup = false/SystemdCgroup = true/' > /etc/containerd/config.toml
        - systemctl restart containerd && systemctl enable containerd
        - echo 'runtime-endpoint: unix:///run/containerd/containerd.sock' > /etc/crictl.yaml
        - curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.35/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
        - echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.35/deb/ /" > /etc/apt/sources.list.d/kubernetes.list
        - apt-get update && apt-get install -y kubelet kubeadm kubectl && apt-mark hold kubelet kubeadm kubectl
      joinConfiguration:
        nodeRegistration:
          kubeletExtraArgs:
            cloud-provider: ""
            node-labels: "nodepool=cpu"
