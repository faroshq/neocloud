---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: virtualmachines.compute.cloud.platform
spec:
  group: compute.cloud.platform
  names:
    kind: VirtualMachine
    listKind: VirtualMachineList
    plural: virtualmachines
    shortNames:
    - vm
    singular: virtualmachine
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .spec.cores
      name: Cores
      type: integer
    - jsonPath: .spec.memory
      name: Memory
      type: string
    - jsonPath: .spec.gpu.count
      name: GPU
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualMachine is the Schema for the virtualmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineSpec defines the desired state of VirtualMachine.
            properties:
              cloudInit:
                description: |-
                  CloudInit configuration reference. Exactly one of the fields must be set.
                  If not specified, a default cloud-init matching the disk image category is used.
                properties:
                  cloudInit:
                    description: Reference to a CloudInit resource by name (user-owned,
                      cluster-scoped).
                    type: string
                  publicCloudInit:
                    description: Reference to a PublicCloudInit resource by name.
                    type: string
                  secret:
                    description: Reference to a Secret containing cloud-init user-data
                      in the "userData" key.
                    properties:
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: Namespace of the Secret.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                type: object
              cores:
                description: Number of virtual CPU cores (1-64).
                maximum: 64
                minimum: 1
                type: integer
              disk:
                description: Root disk configuration.
                properties:
                  image:
                    description: Base OS image.
                    enum:
                    - ubuntu-22.04
                    - ubuntu-24.04
                    - debian-12
                    - flatcar
                    type: string
                  size:
                    description: Size of the root disk (e.g. "50Gi").
                    type: string
                required:
                - image
                - size
                type: object
              gpu:
                description: Optional GPU configuration.
                properties:
                  count:
                    description: Number of GPU devices (0-8).
                    maximum: 8
                    minimum: 0
                    type: integer
                type: object
              memory:
                description: Amount of memory in Kubernetes resource format (e.g.
                  "4Gi").
                type: string
              ssh:
                description: SSH access configuration.
                properties:
                  enableRootLogin:
                    description: |-
                      EnableRootLogin enables root SSH login with password authentication.
                      When enabled, a root password is either read from RootPasswordSecret
                      or auto-generated and stored in a new Secret (referenced in status.rootPasswordSecret).
                    type: boolean
                  publicKey:
                    description: SSH public key to inject into the VM.
                    type: string
                  rootPasswordSecret:
                    description: |-
                      RootPasswordSecret references a Secret containing the root password in the "password" key.
                      If EnableRootLogin is true and this is not set, a Secret is auto-generated.
                    properties:
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: Namespace of the Secret.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                type: object
            required:
            - cores
            - disk
            - memory
            type: object
          status:
            description: VirtualMachineStatus defines the observed state of VirtualMachine.
            properties:
              conditions:
                description: conditions represent the current state of the VirtualMachine
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              internalIP:
                description: Internal IP address within the platform network.
                type: string
              message:
                description: Human-readable message about the current phase.
                type: string
              phase:
                description: Current lifecycle phase.
                type: string
              relatedResources:
                additionalProperties:
                  description: RelatedResource defines a reference to a related resource.
                  properties:
                    gvk:
                      description: |-
                        GroupVersionKind unambiguously identifies a kind.  It doesn't anonymously include GroupVersion
                        to avoid automatic coercion.  It doesn't use a GroupVersion to avoid custom marshalling
                      properties:
                        group:
                          type: string
                        kind:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - gvk
                  - name
                  type: object
                description: RelatedResources lists resources related to this VM.
                type: object
              rootPasswordSecret:
                description: |-
                  RootPasswordSecret references the Secret containing the root password
                  when EnableRootLogin is true. Set automatically if no RootPasswordSecret
                  was provided in spec.ssh.
                properties:
                  name:
                    description: Name of the Secret.
                    type: string
                  namespace:
                    description: Namespace of the Secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              sshEndpoint:
                description: SSH endpoint (host:port) when VM is running and SSH is
                  configured.
                type: string
              tunnelEndpoint:
                description: Tunnel endpoint for platform proxy access.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
